[project]
name = "crawl4ai-sdk"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "bs4>=0.0.2",
    "crawl4ai",
    "loguru>=0.7.3",
]

[tool.uv.sources]
crawl4ai = { workspace = true }
