# HAWH 爬虫快速指南

## 🚀 快速开始

```bash
uv run hawh_media_sp.py
```

## 📊 爬虫工作原理

```
┌─────────────────────────────────────────────┐
│ 1️⃣  爬取主页面                              │
│   http://www.hawh.cn/hawh/audioVisual/      │
├─────────────────────────────────────────────┤
│ 2️⃣  提取所有 index 链接                     │
│   (艺术→曲艺杂技/index.html)                │
│   (老片→国内经典/index.html)                │
│   (文博→中原访古/index.html)                │
├─────────────────────────────────────────────┤
│ 3️⃣  进入每个栏目页面 (5个)                  │
│   排除footer链接                            │
│   排除导航链接                              │
│   排除再次出现的index链接                   │
│   ↓                                          │
│   提取节目链接                              │
├─────────────────────────────────────────────┤
│ 4️⃣  处理每个节目 (10个)                     │
│   爬取节目详情页面                          │
│   提取媒体链接                              │
│   (mp4, m3u8, flv, mp3等)                  │
├─────────────────────────────────────────────┤
│ 5️⃣  保存数据                                │
│   categories.json (栏目)                    │
│   programs.json (节目)                      │
│   media_links.json (媒体)                   │
│   programs_summary.csv (表格)               │
└─────────────────────────────────────────────┘
```

## 🔧 关键改进

### ✅ Footer 过滤
- 排除 `<footer>` 标签内的链接
- 排除包含 "关于我们"、"联系我们" 等footer关键词的链接
- 排除导航栏链接（首页、看点、产业等）

### ✅ 链接清理
- 排除 `javascript:` 伪协议
- 排除 `#` 锚点链接
- 排除再次出现的 `index` 链接
- 自动URL规范化

### ✅ 去重和验证
- 自动去重（同一URL不重复处理）
- 标题长度验证 (1 < len < 100)
- 有效URL验证

## 📁 输出文件说明

### categories.json
栏目分类列表，包含：
- 栏目名称
- 栏目链接
- 爬取时间

### programs.json
节目详细信息，包含：
- 节目标题
- 节目链接
- 所属栏目
- 媒体文件URL列表
- 媒体数量
- 爬取时间

### media_links.json
只包含有媒体链接的节目，用于快速查找：
- 节目标题
- 栏目名称
- 媒体URL
- 媒体数量

### programs_summary.csv
表格格式，可用Excel打开：
- 栏目 | 节目标题 | 链接 | 媒体数 | 时间

## 🎯 自定义爬取数量

编辑代码中的限制参数：

```python
# run() 方法，第192行
for idx, category in enumerate(categories[:5]):  # 改成你想要的数量
    ...

# run() 方法，第212行
for i, program in enumerate(self.all_programs[:10]):  # 改成你想要的数量
    ...
```

## ⚙️ 配置 Docker 地址

```python
# main() 函数
spider = HAWHMediaSpider(
    docker_url="http://你的docker地址:11235"
)
```

## 🐛 常见问题

**Q: 爬虫卡住了？**
A: 检查Docker服务是否运行，网络连接是否正常

**Q: 没有获取到节目？**
A: 检查网站结构是否改变，可能需要调整正则表达式

**Q: 媒体链接为空？**
A: 节目可能没有嵌入媒体，或媒体格式不在支持列表中

## 📈 性能优化

- 使用异步爬虫，提高效率
- wait_for 机制等待动态加载
- 正则表达式优化匹配速度
- 自动去重避免重复处理

## 📝 许可和注意

⚠️ 遵守网站 robots.txt
⚠️ 合理控制爬虫速度
⚠️ 内容仅供学习使用
